#
# This file is autogenerated by Laragoon
#

docker-compose-yaml: docker-compose.yml

project: wo-insanity

tasks:
  post-rollout:
    - run:
        name: Scaffold the persistent storage # no need to uncomment. safe to run multiple times
        command: /app/scripts/post-deploy.sh
        service: cli
    - run:
        name: Run migrations 
        command: php artisan migrate
        service: cli

environments:
 prod:
#   routes:
#     - nginx:
#       - "some.live.domain.example"
   cronjobs:
     - name: artisan schedule
       schedule: "* * * * *"
       command: cd /app && php artisan schedule:run
       service: cli
